
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "customBuild",
            "type": "shell",
            "command": "cd ${workspaceFolder} && uriscv-cli --config ./config_machine.json --gdb",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}